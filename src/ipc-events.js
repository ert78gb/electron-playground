'use strict'

const checkingForUpdate = 'checking-for-update'
const updateAvalilable = 'update-available'
const updateNotAvailable = 'update-not-available'
const autoUpdateError = 'auto-update-error'
const autoUpdateDownloaded = 'update-downloaded'
const autoUpdateDownloadProgress = 'auto-update-download-progress'
const autoUpdateRestart = 'auto-update-restart'

module.exports = {
  autoUpdate: {
    checkingForUpdate,
    updateAvalilable,
    updateNotAvailable,
    autoUpdateError,
    autoUpdateDownloaded,
    autoUpdateDownloadProgress,
    autoUpdateRestart
  }
}
